import { Given, When, Then } from '@cucumber/cucumber';
Given('in valid <resetLink>', function () {
    return 'pending';
});


When('tries to send an invalid reset link', function () {
    return 'pending';
});

Then('A response is sent with status of {int} and message of invalid reset link messagemake sure the reset link is correct', function (int) {
    return 'pending';
});